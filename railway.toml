[build]
builder = "NIXPACKS"
baseDirectory = "backend_new"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT backend_new.app:app"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10